- ユーザがツールを使うように指示した場合は、 MCP Server は起動/再起動しているものとして実際にツールを使ってください
- 改修が終わったあとに `npm run check && npm run build` が成功することを確認してください
  - `npm run build` が成功してもユーザが MCP Server を再起動しないとあなたがツールを使っても古いコードが実行されたままなので、必ずユーザに再起動を促してください
  - もし、あなたが MCP Server を再起動できる場合は自分で再起動して確認してください (再起動用のツールがある場合)
